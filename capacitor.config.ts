import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ayrash.mobile',
  appName: 'AyRashMobile',
  webDir: 'dist/loan-frontend/browser'
};

export default config;
